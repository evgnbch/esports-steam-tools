# 🖥️ Windows Quick Start Guide

## Пошаговая инструкция для Windows

### 1. 📁 Первый запуск
```batch
# Откройте Command Prompt в папке проекта
cd C:\path\to\esports-steam-tools

# Запустите настройку
windows\SETUP.bat
```

### 2. ⚙️ Настройка конфигурации
```batch
# Откройте файл конфигурации в блокноте
notepad config\.env

# Или используйте любой текстовый редактор
# Заполните данные Steam аккаунтов
```

### 3. 🎮 Запуск инструментов
```batch
# Универсальный инструмент (рекомендуется)
windows\steam-tools.bat

# Или отдельно для каждой игры:
windows\csgo-updater.bat
windows\dota2-updater.bat
```

### 4. 🧪 Тестирование
```batch
# Безопасный тест настроек
tests\windows\TEST-SETUP.bat
```

## ❗ Решение проблем

### Консоль сразу закрывается
- **Решение**: Запускайте .bat файлы из Command Prompt, а не двойным кликом
- **Или**: Добавьте `pause` в конец каждого скрипта (уже исправлено)

### Ошибки с символами
- **Решение**: Все Unicode символы заменены на ASCII (исправлено в последней версии)

### Файл конфигурации не найден
- **Решение**: Сначала запустите `windows\SETUP.bat`
- **Проверьте**: Существует ли файл `config\.env`

### Steam Guard
- **Для автоматизации**: Используйте аккаунты без Steam Guard
- **Или**: Настройте мобильный аутентификатор заранее

## 🔒 Безопасность
- ✅ Все пароли хранятся только локально в `config\.env`
- ✅ Файл `config\.env` защищен от случайных коммитов в Git
- ✅ Никаких данных не передается в интернет

---

**Готово! Теперь инструменты должны работать корректно в Windows.**
