# 🔧 Отладка Windows Scripts

## Проблема: Скрипты закрываются сразу после запуска

### ✅ Исправления внесены:

1. **Добавлена отладочная информация** в каждый скрипт
2. **Исправлены Unicode символы** в конфигурационных файлах  
3. **Добавлен pause** в конец каждого скрипта
4. **Создан тестовый файл .env** с примерными данными

### 🧪 Пошаговое тестирование:

#### 1. Простой тест
```batch
cd C:\path\to\esports-steam-tools
windows\test-simple.bat
```

#### 2. Полная настройка
```batch
windows\SETUP.bat
```

#### 3. Тестирование основных скриптов
```batch
windows\csgo-updater.bat
windows\dota2-updater.bat
windows\steam-tools.bat
```

### 🔍 Отладочная информация

Теперь каждый скрипт показывает:
- ✅ `[DEBUG]` сообщения о каждом шаге
- ✅ Путь к конфигурационному файлу
- ✅ Статус загрузки переменных окружения
- ✅ Подробности об ошибках

### 📁 Файловая структура

Убедитесь что у вас есть:
```
esports-steam-tools/
├── config/
│   ├── .env.example  ✅
│   └── .env          ✅ (создается SETUP.bat)
└── windows/
    ├── SETUP.bat     ✅
    ├── steam-tools.bat ✅
    ├── csgo-updater.bat ✅
    └── dota2-updater.bat ✅
```

### 🚨 Если скрипты всё ещё закрываются:

1. **Запускайте из Command Prompt**:
   ```batch
   cmd.exe
   cd C:\path\to\project
   windows\test-simple.bat
   ```

2. **Проверьте кодировку**: Убедитесь что все файлы в UTF-8

3. **Проверьте права доступа**: Запустите cmd от имени администратора

---

**После этих исправлений скрипты должны работать и не закрываться мгновенно!**
